name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

env:
  DEPLOY_TARGET: github-pages
  NEXT_PUBLIC_DISABLE_NETLIFY_FORMS: "true"
  NEXT_PUBLIC_PRIMARY_SITE_URL: https://repokajian.netlify.app
  NEXT_PUBLIC_PUBLIC_SITE_URL: https://repokajian.netlify.app
  NEXT_PUBLIC_BASE_PATH: /repokajian
  NEXT_PUBLIC_REPO_OWNER: jadwal-kajian
  NEXT_PUBLIC_REPO_NAME: repokajian

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build static export
        run: npm run build:pages

      - name: Add Jekyll opt-out
        run: touch out/.nojekyll

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out
          include-hidden-files: true

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
